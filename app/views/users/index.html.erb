<h2><%= t('user_list') %></h2>
<ul>
  <% @users.each do |user| %>
    <li>
      <%= link_to user.name, user_path(user.id) %>
    </li>
  <% end %>
</ul>
<%= paginate @users %>
<%= link_to t('back_to_books'), books_path %>
